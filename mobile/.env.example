# Chép file này thành `.env`. EXPO_PUBLIC_API_URL trỏ tới BACKEND đang chạy.
#
# ── CÁCH 1 (KHUYẾN NGHỊ cho dev sửa giao diện app từ máy khác) ─────────────
# Trỏ thẳng backend thật trên máy chủ ShopTik. Chạy được ở MỌI nơi, không cần
# chung Wi-Fi, không cần DB, không cần chạy backend ở máy bạn — chỉ cần
# `npx expo start` là app gọi API máy chủ và bạn sửa UI trực tiếp.
EXPO_PUBLIC_API_URL=https://shoptikvn.com
#
# ── CÁCH 2 (chỉ khi tự chạy backend local để test) ────────────────────────
# Dùng IP LAN của máy chạy backend (KHÔNG phải localhost — điện thoại hiểu
# localhost là chính nó). Điện thoại và máy tính phải chung Wi-Fi. Tìm IP bằng
# `ipconfig` (IPv4 Address của card Wi-Fi).
# EXPO_PUBLIC_API_URL=http://192.168.1.x:3000
#
# Giá trị dùng cho bản BUILD nằm ở eas.json, không phải file này.

# --- Đăng nhập bằng Google (không bắt buộc) ---
# Tạo OAuth Client ID ở Google Cloud Console → Credentials. Khai đủ loại nào thì
# đăng nhập trên nền đó chạy; để trống tất cả thì app tự ẩn nút Google.
#   - Web client ID: dùng cho Expo Go và làm "aud" của id_token.
#   - Android/iOS client ID: cho bản build thật (kèm SHA-1 / bundle id tương ứng).
# QUAN TRỌNG: các Client ID này phải được liệt kê trong biến backend
# GOOGLE_OAUTH_MOBILE_CLIENT_IDS thì backend mới chấp nhận id_token.
# EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=xxxxx.apps.googleusercontent.com
# EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID=xxxxx.apps.googleusercontent.com
# EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID=xxxxx.apps.googleusercontent.com
