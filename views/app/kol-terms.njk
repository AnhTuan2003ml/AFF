{% extends "app/app-base.njk" %}

{% macro para(p) %}
  {% set upper = p | upper %}
  {% set isDieu = (p | lower | truncate(5, true, '')) == 'điều ' %}
  {% if isDieu %}
    <h3 class="kol-dieu">{{ p }}</h3>
  {% elif p == upper and (p | length) < 90 %}
    <h2 class="kol-doc-title">{{ p }}</h2>
  {% elif p | truncate(8, true, '') == 'Độc lập' or p == 'Căn cứ:' %}
    <p class="kol-doc-sub">{{ p }}</p>
  {% else %}
    <p>{{ p }}</p>
  {% endif %}
{% endmacro %}

{% block app_content %}
  <link rel="stylesheet" href="/assets/kol.css?v={{ assetVersion }}">
  <section class="kol-wrap kol-terms-wrap">
    <header class="kol-head">
      <span class="kol-eyebrow">ĐĂNG KÝ ĐỐI TÁC · BƯỚC 1/2</span>
      <h1>Thỏa thuận hợp tác KOL/KOC</h1>
      <p>Vui lòng đọc toàn bộ thỏa thuận. Cuộn xuống cuối rồi tích xác nhận để tiếp tục điền hồ sơ.</p>
    </header>

    {% if pendingStatus == 'PENDING' %}
      <div class="kol-status kol-status-pending">
        <b>⏳ Hồ sơ đang chờ duyệt</b>
        <p>Đội ngũ đang xét hồ sơ của bạn. Có kết quả bạn sẽ nhận thông báo.</p>
      </div>
    {% else %}
      {% if pendingStatus == 'REJECTED' %}
        <div class="kol-status kol-status-reject"><b>Hồ sơ trước chưa được duyệt</b><p>Bạn có thể nộp lại sau khi chỉnh sửa.</p></div>
      {% endif %}

      <article class="kol-doc" data-kol-doc>
        <div class="kol-doc-sheet">
          {% for s in sections %}
            {% for p in s.paragraphs %}{{ para(p) }}{% endfor %}
          {% endfor %}
          <span data-terms-end aria-hidden="true"></span>
        </div>
      </article>

      <button type="button" class="kol-jump" data-jump-end aria-label="Xuống cuối điều khoản để xác nhận">
        <span class="kol-jump-tip" aria-hidden="true">Kéo xuống</span>
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 9l6 6 6-6"/></svg>
      </button>

      <form method="get" action="/app/dang-ky-kol/thong-tin" class="kol-accept" id="kol-agree-form">
        <input type="hidden" name="dong_y" value="1">
        <span data-scroll-hint class="kol-accept-hint">↓ Cuộn đọc hết thỏa thuận để tích xác nhận</span>
        <label class="kol-accept-row" for="kol-accept">
          <input type="checkbox" id="kol-accept" required disabled>
          <span class="kol-accept-box" aria-hidden="true"></span>
          <span>Tôi đã đọc và <b>đồng ý</b> toàn bộ Thỏa thuận hợp tác KOL/KOC và Cam kết bảo mật thông tin.</span>
        </label>
        <button class="button button-primary kol-continue" type="submit" id="kol-continue" disabled>Tiếp tục điền hồ sơ →</button>
      </form>
    {% endif %}
  </section>
  <script src="/assets/kol-terms.js?v={{ assetVersion }}" defer></script>
{% endblock %}
